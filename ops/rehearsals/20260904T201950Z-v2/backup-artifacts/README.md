This folder intentionally keeps evidence only.

Removed from version control after the rehearsal:

- raw SQLite backup content
- copied `app.key`
- copied runtime `config.local.php`
- full backup metadata with source paths

Retained in git:

- `SHA256SUMS` as a checksum manifest proving which artifacts were produced
- top-level backup and restore command logs
- restore summary and redacted smoke evidence
